# Sanitizer builds get their own directories, so this is a glob rather than
# "build/" — the lesson dariyakyu's M0 learned the hard way.
build*/

.cache/
.DS_Store
compile_commands.json

# Measurements are regenerated by scripts/, and a committed CSV is a number
# nobody can reproduce. The ones that matter get quoted in BREAK.md instead.
runs/
